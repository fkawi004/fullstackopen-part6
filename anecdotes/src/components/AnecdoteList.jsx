import useStore from '../store'
export default function AnecdoteList() { const anecdotes = useStore(state => state.anecdotes); const filter = useStore(state => state.filter); const vote = useStore(state => state.vote); const remove = useStore(state => state.remove); return [...anecdotes].filter(a => a.content.toLowerCase().includes(filter.toLowerCase())).sort((a,b) => b.votes-a.votes).map(anecdote => <div key={anecdote.id}><div>{anecdote.content}</div><div>has {anecdote.votes} <button onClick={() => vote(anecdote)}>vote</button>{anecdote.votes === 0 && <button onClick={() => remove(anecdote.id)}>delete</button>}</div></div>) }
